# [SOURCE] BirthHub360_Agentes_Parallel_Plan - BudgetFluid
name: BudgetFluid
domain: executivos
cycle: 1
description: Analisa a telemetria de gastos, desvio de previsão e alocações de cenário para otimizar a eficiência orçamentária.

preconditions:
  - Input valido validado contra BudgetFluidInputSchema.
  - Dependencias de engine (tools) configuradas para telemetria e historico.

postconditions:
  - Output strict retornado aderente ao BudgetFluidOutputSchema.
  - Eventos de telemetria despachados.

input_schema:
  type: object
  strict: true
  properties:
    tenantId:
      type: string
    requestId:
      type: string
    targetBudgetEfficiencyPct:
      type: number
    window:
      type: object
      strict: true
      properties:
        startDate:
          type: string
        endDate:
          type: string
    segments:
      type: array
      items:
        type: string
    sections:
      type: array
      items:
        type: string
    constraints:
      type: object
      strict: true
      properties:
        maxActions:
          type: number
        language:
          type: string
        currency:
          type: string

output_schema:
  type: object
  strict: true
  properties:
    agent:
      type: string
      value: BudgetFluid
    domain:
      type: string
      value: executivos
    status:
      type: string
      enum: [success, fallback, error]
    summary:
      type: string
    generatedAt:
      type: string
    budgetBrief:
      type: object
      strict: true
      properties:
        headline:
          type: string
        projectedEfficiencyPct:
          type: number
        recommendedReallocationPct:
          type: number
        signals:
          type: array
          items:
            type: object
            strict: true
            properties:
              metric:
                type: string
              value:
                type: number
              interpretation:
                type: string
              confidence:
                type: string
                enum: [high, medium, low]
        riskSignals:
          type: array
          items:
            type: object
            strict: true
            properties:
              signal:
                type: string
              severity:
                type: string
                enum: [critical, high, medium, low]
              mitigation:
                type: string
        actions:
          type: array
          items:
            type: object
            strict: true
            properties:
              recommendation:
                type: string
              owner:
                type: string
              targetDate:
                type: string
              priority:
                type: string
                enum: [critical, high, medium, low]
    observability:
      type: object
      strict: true
      properties:
        metrics:
          type: object
          strict: true
          properties:
            durationMs:
              type: number
            toolCalls:
              type: number
            toolFailures:
              type: number
            retries:
              type: number
        events:
          type: array
          items:
            type: object
    fallback:
      type: object
      strict: true
      properties:
        applied:
          type: boolean
        mode:
          type: string
        reasons:
          type: array
          items:
            type: string

failure_behavior: degraded_report

fallback:
  strategy: canonical
  retry:
    maxAttempts: 3
    baseDelayMs: 500

raci:
  responsible: BudgetFluid
  accountable: CFO
  consulted: FP&A
  informed: CEO

security:
  access_level: executive-only
  pii_handled: false
  data_retention: transient

observability:
  events:
    - budgetfluid.request.received
    - budgetfluid.contract.loaded
    - budgetfluid.tool.call.started
    - budgetfluid.tool.call.succeeded
    - budgetfluid.tool.call.failed
    - budgetfluid.retry.scheduled
    - budgetfluid.fallback.applied
    - budgetfluid.response.generated
  metrics:
    - duration_ms
    - tool_calls
    - tool_failures
    - retries
  alerts:
    - tool_failures > 5
    - duration_ms > 15000

runtime_enforcement: false
runtime_cycle: 16
